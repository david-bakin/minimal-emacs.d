;;; pre-init.el --- run by minimal-emacs.d _before_ init.el -*- no-byte-compile: t; lexical-binding: t; -*-

;; (I moved the sources since I build emacs - this will help package helpful find them))
(setq source-directory "C:/Drives/Repos/my-tools/emacs/emacs/")
